import SwiftUI

struct UpcomingView: View {
    var body: some View {
        Text("Nessun elemento in programma")
            .foregroundStyle(.secondary)
            .navigationTitle("Prossime")
            .navigationBarTitleDisplayMode(.large)
    }
}
